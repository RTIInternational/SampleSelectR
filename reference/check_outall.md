# Validate the 'outall' argument

Checks that the outall argument is a single logical value.

## Usage

``` r
check_outall(outall)
```

## Arguments

- outall:

  A logical value indicating whether to return the full frame or only
  selected rows.

## Value

Stops execution if validation fails. Invisibly returns TRUE.
